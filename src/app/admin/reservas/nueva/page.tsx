'use client'
import {useState} from 'react'
export default function NewReservation(){const [result,setResult]=useState<any>(null);async function submit(e:any){e.preventDefault();const f=new FormData(e.currentTarget);const entries=Object.fromEntries(f.entries());const body={...entries,guest_count:Number(entries.guest_count)};const r=await fetch('/api/reservations',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});setResult(await r.json())}return <main className="wrap"><div className="card"><h1>Nueva reserva</h1><form onSubmit={submit}><div className="grid"><div><label>Código Airbnb</label><input name="booking_code" required/></div><div><label>Titular</label><input name="holder_name" required/></div><div><label>Entrada</label><input name="check_in" type="date" required/></div><div><label>Salida</label><input name="check_out" type="date" required/></div><div><label>Nº huéspedes</label><input name="guest_count" type="number" min="1" required/></div><div><label>Idioma inicial</label><select name="language"><option value="es">Español</option><option value="en">English</option><option value="de">Deutsch</option></select></div></div><br/><button>Crear reserva y generar enlace</button></form>{result?.guest_url&&<div className="card"><h2>Reserva creada ✓</h2><label>Enlace seguro</label><input readOnly value={result.guest_url}/><p><button onClick={()=>navigator.clipboard.writeText(result.guest_url)}>Copiar enlace</button></p><p className="muted">El token original no se guarda: solo su hash SHA-256.</p></div>}{result?.error&&<p>{result.error}</p>}</div></main>}
