'use client'
import {useState} from 'react'; import {browser} from '../../../lib/supabase'
export default function Login(){const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [msg,setMsg]=useState(''); async function go(e:any){e.preventDefault();const {error}=await browser().auth.signInWithPassword({email,password});if(error)setMsg(error.message);else location.href='/admin'} return <main className="wrap"><div className="card" style={{maxWidth:480,margin:'60px auto'}}><h1>Casa Yaiza · Administración</h1><form onSubmit={go}><label>Email</label><input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/><label>Contraseña</label><input type="password" required value={password} onChange={e=>setPassword(e.target.value)}/><br/><br/><button>Entrar</button><p className="muted">{msg}</p></form></div></main>}
